# Crosswords

Interactive crossword generation and play UI under `/crosswords`.

- `generator.py`, `word_bank.py`, and `word_source.py` implement puzzle generation.
- `/api/new` creates a puzzle from the requested theme and difficulty.
- Frontend assets live in `static/`; the page template lives in `templates/`.
