# Proxy

Authenticated web browsing proxy under `/proxy`.

- URL validation and response rewriting are implemented in `__init__.py`.
- The browsing page and subapp styling live in `templates/` and `static/`.
- Preserve host and scheme validation when changing navigation behavior.
