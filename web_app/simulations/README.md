# Simulations

Browser-based interactive demonstrations under `/simulations`.

The current simulations are Conway's Game of Life and A* pathfinding. Each has a template and corresponding JavaScript in `static/`; shared subapp styling lives in `static/style.css`.
