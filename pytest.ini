[pytest]
markers =
    integration: marks tests as integration tests
    ui: marks tests as UI tests (requires playwright)
    ffmpeg: marks tests that require ffmpeg and ffprobe
testpaths = tests/unit tests/integration
